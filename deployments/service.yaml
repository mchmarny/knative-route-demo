apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: blue-green-demo
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: chhsiao/knative-route-demo
  traffic:
    - percent: 50
    - revisionName: blue-green-demo-XXXXX
      percent: 50
