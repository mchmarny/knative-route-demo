<!DOCTYPE html>
<html lang="en">
<head>
    <title>Demo</title>
    <link rel="stylesheet" type="text/css" href="/css/app.css" />
</head>
<body>
        {{ if eq .version "green" }} 
            <div class="green">v2</div>
        {{else}}
            <div class="blue">v1</div>
        {{end}}
    </div>
</body>
</html>